server:
  port: 443
  ssl:
    key-store: classpath:/ssl/blogssl.pfx
    key-store-password: 1535651784881
    key-store-type: PKCS12
    
spring:
    devtools:
        enabled: true
    mvc: 
      static-path-pattern: /**
    resources:
      static-locations: classpath:/static,classpath:/public,classpath:/resources,classpath:/META-INF/resources,file:${site.store.root}
    http:
        encoding:
          charset: UTF-8
          force: true
          enabled: true
    freemarker:
        template-loader-path:
        - classpath:/templates
        - file:./templates
        cache: false
        checkTemplateLocation: false
        settings:
            classic_compatible: true #处理空值
            template_exception_handler: rethrow
            template_update_delay: 0
            datetime_format: yyyy-MM-dd HH:mm
            number_format: 0.##

    datasource:
        driver-class-name: com.mysql.jdbc.Driver
        url: jdbc:mysql://rm-2ze970347k5e98h0uqo.mysql.rds.aliyuncs.com:3306/uzblog?useSSL=false&characterEncoding=utf8
        username: mantester
        password: mantester

    jpa:
        database: mysql
        show-sql: false
        hibernate:
            ddl-auto: update
        properties:
            hibernate.format_sql: true
            hibernate.naming.physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
            hibernate.cache.use_second_level_cache: false
            hibernate.search.default.directory_provider: filesystem
            hibernate.search.default.indexBase: ${site.store.root}/indexes

    servlet:
        multipart:
            max-file-size: 5MB

    mail:
        host: smtp.exmail.qq.com
        username: uzzz@ibz.bz
        password: HKi5e8JE6CCd7KsW
        port: 25
        properties:
            mail.smtp.auth: true

# site.theme: [default|card]
site:
    theme: card
    store:
      root: D:/data/uzblog/
      size: 2